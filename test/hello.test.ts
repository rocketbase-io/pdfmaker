import "mocha";
import {expect} from "chai";

describe('services', () => {

  describe('pdf', () => {

    it('should produce a pdf document for a valid json payload', async () => {

      expect('hello').to.eq("hello");
    })

  });

});
